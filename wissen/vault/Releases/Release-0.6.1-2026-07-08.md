---
titel: "Release 0.6.1"
tags: [release, "v0.6.1"]
status: "veroeffentlicht"
erstellt: "2026-07-08"
verwandt: ["[[Release-Ablauf]]"]
---
# Release 0.6.1

Automatisch erzeugt aus `ROADMAP.md` (Einträge ab **213**) von
`kosmo-orbit/tools/release-notiz.mjs` — Teil des Release-Ablaufs
[[Release-Ablauf]] (Owner-Auftrag v0.6.2: «bei jedem Update pushe alles auf
git, obsidian und die neuste Installer-Version zum Herunterladen auf der
Website»).

## Enthaltene ROADMAP-Einträge (213–226)

- **213.** Nacht v0.6.2 / D1: Zonenregel speist die Volumenstudie (Sonnet gegen Fable-Spec, Review Fable)
- **214.** Nacht v0.6.2 / D2: Besonnungs-Kennwert je Extremvariante (Sonnet, Review Fable)
- **215.** Nacht v0.6.2 / D3: Raumprogramm-Erfüllung je Extremvariante (Sonnet, Review Fable)
- **216.** Nacht v0.6.2 / D4: Kosmo löst die Extremvarianten-Studie selbst aus — `grundlagen.volumenstudie` (Sonnet gegen Fable-Spec, Review Fable). Die D-E9-Architektur
- **217.** Nacht v0.6.2 / B1: Adaptions-Kern stationsneutral + Rollout auf KosmoData — «system-weit» beginnt beweisbar (Sonnet gegen Fable-Spec, Review Fable). Serie J2, docs/SERIE-J2-IMMERSIVE-OBERFLAECHE.md Abschnitt 3.1/4
- **218.** Nacht v0.6.2 / D5: Grundlagenstudie-Bericht — ein druckfähiges SVG-Artefakt aus der Studie (Sonnet gegen Fable-Spec, Review Fable). WETTBEWERB-KONZEPT D-E8
- **219.** Nacht v0.6.2 / D6: Wettbewerbs-Simulationslauf `sim-wettbewerb.spec.ts` — die Grundlagenphase läuft end-to-end (Sonnet gegen Fable-Spec, Review Fable)
- **220.** Nacht v0.6.2 / C5: Ehrlicher PDF-Drop-Pfad beim Unternehmerplan-Import (Sonnet gegen Fable-Spec, Review Fable). SUBMISSION-KONZEPT C5
- **221.** 0.6.1-Update — Nachtbatch ausgeliefert + Rundgang-PDF + ehrlicher Volllauf-Befund (Owner-Entscheid: 0.6.2 ist für die Notizen-Runde reserviert)
- **222.** v0.6.2 / Vorform-Tiefenkonzept — `docs/VORFORM-KONZEPT.md` (Sonnet-Recherche gegen Fable-Spec, Review Fable)
- **223.** v0.6.2 / Finch-Tiefenkonzept — `docs/FINCH-KONZEPT.md` (Sonnet-Recherche gegen Fable-Spec, Review Fable)
- **224.** v0.6.2 / LoRA-Konzept + Trainingsdaten-Start — `docs/LORA-KONZEPT.md` + Obsidian-Vault-Struktur + erste echte Stichprobe (Sonnet gegen Fable-Spec, Review Fable)
- **225.** v0.6.2 / KosmoVis ohne HomePC — `docs/KOSMOVIS-OHNE-HOMEPC.md` (Sonnet-Recherche mit Sub-Agenten gegen Fable-Spec, Review Fable mit Nachtrag)
- **226.** v0.6.2 / I2 — Sicherer-Standard-Batch: Bridge-Fail-Closed + Secret-Scan über wissen/ (Sonnet gegen Fable-Spec, Review Fable)

## Installer

Stabile Download-Links (immer der zuletzt gebaute Installer, drei Editionen ×
drei Plattformen): [[Release-Ablauf]] Abschnitt 4, live auf
architekturkosmos.ch/orbit — sobald die Website-Änderung selbst auf `main`
liegt (siehe [[Release-Ablauf]] Abschnitt 6, DEPLOYMENT.md).

## Volltext je Eintrag

### 213

213. **Nacht v0.6.2 / D1: Zonenregel speist die Volumenstudie (Sonnet gegen Fable-Spec, Review Fable)** ✅: Entscheid D-E2 — der D0-Befund «maxHoeheM hartcodiert» ist behoben. Kernel: `studienOptionenAusRegel(regel, parzelleFlaecheM2)` (rein, exactOptionalPropertyTypes-sauber) leitet NUR ab, was die Regel wirklich hergibt: maxHöhe direkt, zielGf = az × Parzellenfläche (exakt die maxAgf-Formel aus `design.zonenRegelSetzen` — kein zweiter Rechenweg), Grenzabstand aus grenzabstandKlein; maxVollgeschosse/grenzabstandGross ehrlich NICHT verdrahtet (kein StudienOptionen-Feld). App: StudienPanel initialisiert Regler aus der Regel (bleiben Override), Hinweis `studie-regel-hinweis` «aus Zonenregel ‹Name›», Grenzabstand-Label zeigt den echten Wert; ohne Regel byte-identisches Verhalten (Guard-bewiesen). Kernel +7 Tests.

### 214

214. **Nacht v0.6.2 / D2: Besonnungs-Kennwert je Extremvariante (Sonnet, Review Fable)** ✅: Neu `derive/besonnungsvergleich.ts` (rein): echte Sonnenstände via SunCalc (dieselbe Bibliothek wie die 3D-Schattenstudie, jetzt explizite Kernel-Dependency), Wintersonnenwende 10/12/14 Uhr MEZ, Schattenwurf je Studien-Körper als konvexer Sweep (exakt für die Rechteck-Körper des Generators), `union()`-verschmolzen, beschattete Fläche in m² + Richtwert-Mittel. **Ehrlich als Konstante im Code**: Vergleichs-RICHTWERT zwischen Varianten, kein Normnachweis, kein Ersatz der echten Schattenstudie; Grenzen (keine Selbstverschattung, Klammer bei Sonnenhöhe <3°) im Header. Kernel +6 Tests (Turm vs. Riegel gleicher GF unterscheidet sich richtungsplausibel zu allen drei Zeiten). **Beifang-Befund dokumentiert**: `Viewport3D.tsx`-Kommentar behauptet Radiant/Süd-Konvention, suncalc@2 liefert real Grad/Nord — Nachprüfung als eigener Posten.

### 215

215. **Nacht v0.6.2 / D3: Raumprogramm-Erfüllung je Extremvariante (Sonnet, Review Fable)** ✅: Neu `derive/programmerfuellung.ts` (rein): Gesamt-GF der Studie vs. Soll (Σ hnfSoll × programmFaktor — DERSELBE Faktor-Weg wie `deriveBerechnungsliste`, kein eigener erfunden), Quote in % (null bei Soll 0), Delta absolut, **Ehrlichkeits-Hinweis auf jeder Zeile** «Gesamt-GF-Vergleich — keine Raumtypen-Zuordnung (Studie)» (Entscheid D-E5, StudienKoerper.program bleibt 'studie'). Kernel +8 Tests (100 %/50 %/Summen/leer/Determinismus/Integration mit generiereVolumenstudien). Gemeinsames Welle-1-Gate: Typecheck 0 Fehler, Kernel 294→**315**, App 361, Build grün, **Goldens byte-identisch**.

### 216

216. **Nacht v0.6.2 / D4: Kosmo löst die Extremvarianten-Studie selbst aus — `grundlagen.volumenstudie` (Sonnet gegen Fable-Spec, Review Fable). Die D-E9-Architektur** ✅: Der D0-Kernbefund (Kosmo hatte keinen Tool-Zugriff auf Ableitungen) ist geschlossen. **Neu `commands/grundlagen.ts`**: löst Parzelle (letzte Zone im Geschoss, wie das StudienPanel) + Zonenregel/Parzellenfläche auf, `studienOptionenAusRegel` + Overrides, `generiereVolumenstudien`, wählt Variante (Index oder GF-nächste) und materialisiert die Körper als MassBody-Patches — EIN Aufruf = EINE atomare Undo-Gruppe; ehrliche CommandErrors (keine Zone / kein zielGf ableitbar / Index out-of-range). Da registriert = automatisch Kosmo-Tool. **Architektur-Erweiterung (rückwärtskompatibel)**: `summarize(params, doc)` — Diff-Karten können jetzt berechnete Zahlen zeigen (GF, Erfüllung aus `programmErfuellungJeVariante`); durchgezogen bis `validateToolCall`/`chat.ts`. **Echter Beifang-Bug gefixt**: `commandIdFor` in kosmo-ai kannte nur `design_`/`doc_`-Präfixe — `publish.*`/`vis.*`-Tools waren als Kosmo-Tools gelistet, aber NIE auflösbar («Unbekanntes Werkzeug»); generischer Fix, Roundtrip über alle 76 Commands verifiziert, Regressionstest. Tests: Kernel 315→**327** (+12 inkl. atomarem Undo/Redo), KI 52→**54** (+2: Tool-Erreichbarkeit end-to-end + Roundtrip). Alle Suiten + Build grün, Goldens byte-identisch.

### 217

217. **Nacht v0.6.2 / B1: Adaptions-Kern stationsneutral + Rollout auf KosmoData — «system-weit» beginnt beweisbar (Sonnet gegen Fable-Spec, Review Fable). Serie J2, docs/SERIE-J2-IMMERSIVE-OBERFLAECHE.md Abschnitt 3.1/4** ✅: Die Fokus-/Adaptions-Systematik (Serie J / J3a–J3c) war KosmoDesign-verdrahtet — jetzt ist der stationsneutrale Teil als **`state/oberflaeche-adaption-kern.ts`** extrahiert: Storage-Layer (`kosmo.adaption.v1`, geteilt über Stationen), Rang-/Opazitäts-Helfer, die drei Primitive `stufeAusRegel`/`wendeAntiDimmAn`/`wendeNutzerHebungAn`, `darfUmordnen` und der generische Hook `useAdaptionsSteuerung` (Freeze/Debounce, Opt-out, Element-Hebung). **`oberflaeche-adaption.ts` ist nur noch der Design-Adapter** — exakt dieselben Exporte/Signaturen, `DesignWorkspace.tsx` mit **Null Diff**, alte Design-E2E unverändert grün (der härteste Beweis, dass die Extraktion verhaltensidentisch ist). **Zweite Station real**: `oberflaeche-adaption-data.ts` (Gruppen `navigation/suche/sync/dossier` mit eigener B1-Matrix) + `DataWorkspace.tsx` verdrahtet (Leisten-Gruppen, Adaptions-Schalter/-Reset neben Sync, `nutzungMelden` auf Tab-/Sync-/Dossier-Aktionen) — dasselbe gelernte Profil wirkt jetzt in zwei Stationen. Ehrliche Abweichung dokumentiert: `wendeAntiDimmAn` nimmt eine Ziel-Stufe statt nur der Gruppen-Basis (Suche/Dossier müssen beim Tippen auf `primaer` über ihre eigene Basis hinaus). Tests: App 361→**411** (+50: Kern 33, Data 17), neue E2E `oberflaeche-adaption-data.spec.ts` (6 Tests) 3× isoliert grün, alte `oberflaeche-adaption.spec.ts` (6) unverändert grün, Kernel 327/Goldens byte-identisch, Typecheck + Build sauber.

### 218

218. **Nacht v0.6.2 / D5: Grundlagenstudie-Bericht — ein druckfähiges SVG-Artefakt aus der Studie (Sonnet gegen Fable-Spec, Review Fable). WETTBEWERB-KONZEPT D-E8** ✅: **Neu `derive/studienbericht.ts`**: `studienBerichtSvg(varianten, opts)` — reine, deterministische Ableitung (kein `Date.now`, Datum kommt vorformatiert von der App) → eigenständiges A4-quer-SVG: Kopf (Titel, «aus Zonenregel …», Datum, Ziel-GF), je Variante eine Spalte mit Mini-Footprint (alle Körper-Polygone, gemeinsamer Massstab über die globale BBox — grössere Varianten sind sichtbar grösser, bewusst KEINE fake 3D-Perspektive) + Kennwerten (Geschosse, Höhe, GF, Warnungen, Grenzabstand-Besonnung, optional Winter-Besonnung/Programm-Erfüllung). **Ehrlichkeitspfad hart**: Besonnungs-/Programm-Kennwerte und ihre Hinweise (`BESONNUNG_HINWEIS`/`PROGRAMM_ERFUELLUNG_HINWEIS`) erscheinen NUR, wenn Standort bzw. Raumprogramm wirklich im Doc stehen; fehlende Werte zeigen «—», nie Platzhalterzahlen; Fusszeile immer «Anstoss, kein Entwurf». StudienPanel-Knopf «Bericht (SVG)» lädt `grundlagenstudie.svg`. Dabei `besonnungsvergleich`/`programmerfuellung` (D2/D3) ans Kernel-Barrel angeschlossen — vorher bewusst offen, jetzt vom ersten echten Konsumenten gebraucht. Tests: Kernel 327→**340** (+13, inkl. Determinismus byte-identisch + neues Golden `studienbericht.svg`; alte Goldens unangetastet), App 411 grün, neue E2E `studienbericht.spec.ts` (3 Tests: Download-Inhalt, Ehrlichkeitspfad ohne Standort/Programm, voller Pfad mit beidem) 3× isoliert grün, `sim-vollhaus` gegengeprüft grün.

### 219

219. **Nacht v0.6.2 / D6: Wettbewerbs-Simulationslauf `sim-wettbewerb.spec.ts` — die Grundlagenphase läuft end-to-end (Sonnet gegen Fable-Spec, Review Fable)** ✅: Vier Akte nach der WETTBEWERB-KONZEPT-Dramaturgie: (1) **Grundlagen** — Projekt, Zonenregel «W4 (Zürich-Altstetten)» mit realer Parzellenfläche (960 m² aus dem 40×24-Umriss), Parzelle als Zone, Raumprogramm; (2) **Kosmo-Studie** — `grundlagen.volumenstudie` (D4) über den `__kosmo.run`-Weg, Assertions: MassBody-Delta > 0, ALLE neuen Körper `program:'studie'`, EIN Undo entfernt ALLE (atomare Gruppe bewiesen, Poll auf 0), Redo bringt dieselben IDs zurück (Set-Vergleich); (3) **Matrix** — `studie-regel-hinweis` mit Regelnamen, Parallel-Axis-Matrix mit ≥2 Linien (real 3 — Zeilen/Winkel/Blockrand fallen für diesen Zuschnitt geometrisch weg, ehrlich nicht erzwungen); (4) **Bericht** — `grundlagenstudie.svg`-Download (D5) enthält Regelnamen + Programm-Erfüllung + «Anstoss, kein Entwurf». **Baustein 20 `grundlagenStudieAusfuehren`** append-only an die eingefrorene Baustein-API angehängt (1–19 unverändert). Ehrliche Befunde dokumentiert: `regelpresets.ts` hält nur Raumtyp-Richtwerte, keine Zonenrecht-Presets (Regel kommt aus dem Szenario, wie bei allen Journeys); kein Redo-Testid im Produkt (Text-Locator, etablierte Praxis). Gates: Spec 3× isoliert grün, `studienbericht`+`sim-submission` als Verwandte grün, Kernel 340/App 411 unberührt grün, Typecheck + Build sauber.

### 220

220. **Nacht v0.6.2 / C5: Ehrlicher PDF-Drop-Pfad beim Unternehmerplan-Import (Sonnet gegen Fable-Spec, Review Fable). SUBMISSION-KONZEPT C5** ✅: Unternehmer schicken real oft PDF statt DXF — jetzt geht ein PDF beim «DXF laden» NICHT mehr in den Parser: Erkennung Endung-zuerst + `%PDF`-Magic-Bytes-Fallback (fängt auch ein PDF mit falscher `.dxf`-Endung ab, statt Parser-Kauderwelsch). **Neu `unternehmerplan-pdf.ts`**: pures Betriebsarten-Gate `pdfImportPfad(betriebsLage)` — Cloud+Anthropic+Zugang → Pfad `vision-anfrage` zulässig, alles andere → ehrlicher `hinweis` (was das PDF ist, warum ohne Cloud-KI keine automatische Analyse, konkreter Arbeitsweg «DXF beim Unternehmer anfordern — R12/AC1009 wird gelesen»), im Panel als `pdf-hinweis` sichtbar. **Ehrlich dokumentierte Lücke statt Halb-Feature**: der kosmo-ai-Provider kann heute nur Text-Content-Blöcke (`ChatMessage.content: string`, kein image/document) — `anthropicUnterstuetztPdfVision()` liefert darum ehrlich `false`, und selbst Cloud+Schlüssel bekommt «PDF-Analyse braucht Vision-Unterstützung des Providers — noch nicht angeschlossen». NIRGENDS wird ein Befund erfunden; `baueHypothesenKarten` (Vision-Antwort → IMMER Stufe-2-Hinweis-Karten, nie Stufe 1) liegt als getestete Infrastruktur für den Anschluss-Tag bereit. Tests: App 411→**433** (+22), neue E2E `unternehmerplan-pdf.spec.ts` (2: Ehrlichkeitspfad + Magic-Bytes bei falscher Endung) 3× isoliert grün, bestehende Unternehmerplan-E2E grün, Kernel 340/Goldens unberührt, KI 54 unverändert.

### 221

221. **0.6.1-Update — Nachtbatch ausgeliefert + Rundgang-PDF + ehrlicher Volllauf-Befund (Owner-Entscheid: 0.6.2 ist für die Notizen-Runde reserviert)** ✅: (a) **Owner-Entscheid** am Morgen: der Nacht-Stand (ROADMAP 213–220) wird als aufgefrischtes **0.6.1** gebaut und verschickt; die Nummer 0.6.2 bekommt die gemeinsame Runde mit den Owner-Notizen. Versions-Bump zurückgenommen, «0.6.1» im Bundle-Chunk nachgewiesen. (b) **Ehrlicher Befund aus dem Release-Volllauf** (erstmals wirklich ALLE 167 E2E-Tests): 19 rot — Ursache NICHT die Nacht-Features (alle grün), sondern ein latenter Block-E-Kollateralschaden: der Starter-Guide startet seit `7b1d0b9` automatisch, seine nicht-modale Karte fängt Klicks unter sich ab (nav-fit, Plansatz-Export — darum feuerten auch die Download-Events nie), und die betroffenen Alt-Specs (eingabe-3d, freemesh, sketch-3d-a4, module teilweise, 5 Sim-Journeys) liefen seit Block E nie mehr im Volllauf. Fix: `kosmo.starterGuide.done` im Test-Bootstrap (Specs + Baustein 1, interner Fix ohne API-Bruch; ein Test brauchte zusätzlich das `reload()`-Muster, weil das Flag beim App-Start gelesen wird). Verifikation: die 19 isoliert grün, danach ALLE berührten Spec-Dateien komplett neu: **103 passed + 1 STT-Skip** — zusammen mit den 147 unberührt grünen ist die Suite auf aktuellem Stand vollständig belegt. Produktcode unverändert. (c) **Rundgang-PDF zum Kommentieren** (Owner-Wunsch): neu `e2e/tools/rundgang.mts` (21 frische Full-HD-Screenshots inkl. Nacht-Features: Studien-Panel mit Regel-Hinweis+Matrix, Grundlagenstudie-Bericht-SVG, Unternehmerplan-PDF-Hinweis, KosmoData-Adaption) + `rundgang-pdf.mts` → `abgabe/RUNDGANG-NOTIZEN-0.6.1.pdf` (18 Seiten A4: je Station Screenshot, Beschrieb, linierte Notiz-Box; Deckblatt mit Anleitung). In den Chat geliefert; die Notizen werden die 0.6.2-Auftragsliste. (d) Build-Trigger für die Desktop-Installer mit diesem Commit.

### 222

222. **v0.6.2 / Vorform-Tiefenkonzept — `docs/VORFORM-KONZEPT.md` (Sonnet-Recherche gegen Fable-Spec, Review Fable)** ✅: Owner-Auftrag «Vorform auseinandernehmen, Grundkonzept finden, Konzept bauen». 281 Zeilen, vertieft `RE-VORFORM.md` statt es zu wiederholen. Kernbefunde: (1) der alte V1–V8-Batch-Plan ist bereits gebaut (nur CH-Standort-Kontext offen) — die Aufgabe war Konzept-Ebene, nicht Feature-Jagd; (2) Grundkonzept präzisiert auf DREI Eigenschaften: reibungsarmes Modellieren + Sofortigkeit des Bildes IM SELBEN FENSTER + Stilkonstanz — beim Modellieren ist KosmoOrbit schon gleichwertig/besser (FreeMesh Stufe 3, Volumenstudien-Generator, den Vorform gar nicht hat); (3) die reale Lücke ist erlebnisbezogen: KosmoVis ist ein eigener Node-Workspace, kein Ein-Klick-Render im 3D-Viewport; (4) stilkonstantes AI-Imaging hat KEINES der sechs Vergleichstools (Hypar/TestFit/Spacio/Veras/LookX/Maket) öffentlich gelöst → echte Differenzierungschance via LORA-KONZEPT statt Nachbau. Batch-Plan V-M1…V-M5 (Render-Knopf im Viewport → Stil-Anker je Projekt → Sofort-Vorschau 🔒 Bridge → Cloud-Imaging 🔒 Schlüssel → CH-Kontext). Ehrlich: kein Binary-RE, Vorform ist invite-only — Qualitätsaussagen bleiben als Hypothese markiert; alle Web-Quellen mit URL, alle Code-Aussagen mit Dateipfad.

### 223

223. **v0.6.2 / Finch-Tiefenkonzept — `docs/FINCH-KONZEPT.md` (Sonnet-Recherche gegen Fable-Spec, Review Fable)** ✅: Owner-Auftrag «Finch auseinandernehmen, Grundkonzept finden, Konzept bauen». 364 Zeilen, vermisst den Ist-Stand NEU statt `RE-FINCH.md` zu wiederholen — dessen Bau-Plan F1–F10 ist seit 03.07. komplett gebaut (ROADMAP 49–66) plus ungeplante Zugaben (Grundriss-Generator, Zonen→Wände, Grundlagenstudie D1–D6). Zweite Recherchewelle (docs.finch3d.com: algorithm-theory/new-features/enterprise/adaptive-plan-library + Graph-Rules-Post) bestätigt das RE-Dossier fast wörtlich — Antwort auf «wie tief graben»: nicht tiefer in Finch, sondern in (a) akademische Graph-Floorplan-Literatur (Graph2Plan, House-GAN/++, WallPlan — letzteres methodisch näher an unserer Zonen→Wände-Pipeline als an Finchs Box-Modell), (b) eigene CH-Normtiefe, (c) eigene Projektdaten für die LoRA. **Lücken-Matrix hart**: von 17 Fähigkeiten nur noch 3 echte ❌ (Korridor-/Kern-Autofindung, Plan Groups als verknüpfte Instanzen, Enterprise-KI «im Bürostil» = LoRA-Terrain), 8 ◐. Batch-Plan FG1–FG9. Beifang: Auftrags-Irrtum korrigiert (ZonenVorlage lebt in model/doc.ts, nicht entities.ts). Alle Quellen mit URL, Code-Belege mit Pfad, Ehrlichkeits-Abschnitt (Metaheuristik-These bleibt Rekonstruktion — Finch publiziert nichts).

### 224

224. **v0.6.2 / LoRA-Konzept + Trainingsdaten-Start — `docs/LORA-KONZEPT.md` + Obsidian-Vault-Struktur + erste echte Stichprobe (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner-Auftrag «erste LoRA-Runde für Grundrissgenerierung und AI-Imaging; Training-Vorbereitung mit Claude Code; Wissen im Obsidian-Vault speichern». **Ehrlichkeits-Rahmen hart**: kein GPU im Container — der eigentliche QLoRA-/Stil-LoRA-Lauf ist 🔒 HomeStation (RTX 5090); hier entsteht alles bis dorthin. **Ziel A (Grundriss)**: SFT-Chat-Format (Raumprogramm → {raeume, moebel, tueren, kennzahlen, diagnose}), Datenquelle = der EIGENE Kernel-Generator; **echte Stichprobe** `wissen/training/lora/grundriss-v0.jsonl` (29 Beispiele, per Wegwerf-Skript direkt aus `generiereGrundriss()`/`generiereGrundrissL()` erzeugt: 24 aus 96er-Parameter-Grid mit Qualitäts-Gate nach checks.ts-Schwere, 3 bewusste Verweigerungs-Randfälle — ehrliches Nein statt Erfinden gehört in die Trainingsdaten —, 2 L-Grundrisse; 29/29 JSON-valide). **Ziel B (Imaging-Stil)**: kohya-Rezept + Captioning-Konvention; ehrlich benannt: KEIN Bildkorpus im Container, Bild-Charge = Owner-Entscheid. **Obsidian**: `wissen/vault/LoRA/` mit Übersicht, zwei Ziel-Notizen und Trainingslauf-Vorlage (Frontmatter + [[Wikilinks]]) — erste eigenständig verfasste Vault-Untermenge. 5 offene Owner-Entscheide dokumentiert (SDXL vs. Flux, Stil-Richtung, Bild-Herkunft, leere Plan-Library als Gold-Quelle, Modellnamen-Schema).

### 225

225. **v0.6.2 / KosmoVis ohne HomePC — `docs/KOSMOVIS-OHNE-HOMEPC.md` (Sonnet-Recherche mit Sub-Agenten gegen Fable-Spec, Review Fable mit Nachtrag)** ✅: Owner-Frage «Wie machen wir KosmoVis besser ohne den Home-PC?». Kernbefund aus dem Inventar: KosmoVis rendert heute KEIN Pixel selbst — es ist ein Job-Client zur HomeStation; im Cloud-Betrieb heisst das wörtlich «kein Render». **Drei ehrliche Optionen**: (a) **In-App-Qualitätspfad** (sofort, gratis, privat): three.js-Bordmittel im Viewport aufwerten — PMREM-Umgebungslicht, ACES-Tonemapping, GTAO/SSAO, TAA-Akkumulation (alles WebGL2, kein Pathtracer nötig; three-gpu-pathtracer als optionale Stufe, im Tech-Radar längst ADOPT aber nie verdrahtet); WebGPU bewusst NICHT: macOS erst ab Safari 26/Tahoe, WKWebView ohne App-Schalter, WebKitGTK/Linux gar nicht (Maintainer-Aussage verifiziert) — nur Windows/WebView2 hätte es. (b) **Cloud-GPU mit Schlüssel**: Flux-Familie bei Replicate/fal.ai, live verifizierte Preise $0.003–$0.06 pro Bild — technisch machbar, aber das Modell verlässt das Büro → Entscheidungsvorlage an den Owner, keine Technikfrage. (c) ehrlich HomeStation (🔒): Cycles gratis+privat, LoRA-Inferenz, grosse Batches. **Empfohlener erster Batch «In-App-Renderqualität»** (Viewport3D: Umgebungslicht/Tonemapping/AO, kein Kernel-Diff, Goldens unberührt, SwiftShader-testbar). Richtigstellung im Dokument: Anthropic/Claude erzeugt keine Bilder. Review-Nachtrag Fable: Preistabelle und WebKitGTK-Beleg aus der zweiten Recherche-Runde hart nachverifiziert (inkl. Korrektur einer falschen fal.ai-Zahl).

### 226

226. **v0.6.2 / I2 — Sicherer-Standard-Batch: Bridge-Fail-Closed + Secret-Scan über wissen/ (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner «Sicherheitsmassnahmen ausbauen». **Ehrliche Befund-Korrektur zuerst**: der Fable-Verdacht «Bridge im Netz offen» war kleiner als der Wortlaut — der Bind-Default war seit B4 schon `127.0.0.1`; real falsch war die MELDUNG (feuerte «im Netz offen» auch bei rein lokalem Bind) und der stillschweigend erlaubte Fall «nicht-lokaler Host OHNE Token». Neu: reine, testbare `bind_entscheidung()` — lokal → präzise Meldung; LAN+Token → wie gehabt; **LAN ohne Token ohne Bestätigung → Start VERWEIGERT** (Exit 1); bewusster Offen-Betrieb nur noch explizit via `--offen-ohne-token`/`KOSMO_BRIDGE_OFFEN=1` mit unübersehbarem Warnbanner. Prinzip: sichere Standards, laute Ausnahmen. **Zweiter echter Fund**: `tools/secret-scan.mjs` prüfte `wissen/` gar nicht (lag ausserhalb der Scan-Wurzel) — jetzt `wissen/vault` + `wissen/training` inkl. `.jsonl` im Scan (0 Funde). Doku: I2-Nachtrag in SERIE-I-SICHERHEIT.md mit Betriebsmatrix; Tauri-CSP/Allowlist für die neuen DXF/PDF-Import-Pfade verifiziert unverändert nötig-frei (reine Browser-File-API); `npm audit --omit=dev` 0 Findings (nichts «gefixt», was nicht kaputt war). Gates: Bridge-Härtetests +6 grün (inkl. echter Prozessläufe: Verweigerung, Banner, Default lokal), Secret-Scan-Tests +4 grün, Kernel 340/App 433 unberührt, `homestation-kette`-E2E 2/2 mit unverändertem Fake-Betrieb.

