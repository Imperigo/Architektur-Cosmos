---
titel: "Release 0.6.3"
tags: [release, "v0.6.3"]
status: "veroeffentlicht"
erstellt: "2026-07-09"
verwandt: ["[[Release-Ablauf]]"]
---
# Release 0.6.3

Automatisch erzeugt aus `ROADMAP.md` (Einträge ab **232**) von
`kosmo-orbit/tools/release-notiz.mjs` — Teil des Release-Ablaufs
[[Release-Ablauf]] (Owner-Auftrag v0.6.2: «bei jedem Update pushe alles auf
git, obsidian und die neuste Installer-Version zum Herunterladen auf der
Website»).

## Enthaltene ROADMAP-Einträge (232–251)

- **232.** v0.6.3 / Notion-Scan-Auswertung — 16 KI-Recherche-Scans dedupliziert, angedockt, ehrlich begrenzt (Sonnet gegen Fable-Spec, Review Fable)
- **233.** v0.6.3 / VP1 — SIA-Teilphasen-Modell: der reale Projektstand zieht ins Doc ein (Sonnet gegen Fable-Spec, Review Fable)
- **234.** v0.6.3 / Serie K A1 (K11) — Kosmo als Copilot-Symbol, nicht Dauerchat (Sonnet gegen Fable-Spec, Review Fable). Start der sichtbaren UI-Transformation aus dem Owner-PDF
- **235.** v0.6.3 / Serie K A2 (K12) — Zentrale-Startmenü: Tiefenlayer, Werkzeug-Hover, Info-Icon, Start-/Idle-Animation (Sonnet gegen Fable-Spec, Review Fable)
- **236.** v0.6.3 / Serie K A3 (K13) — Erster Start: Kosmo fragt «Neu hier?» statt Guide-Autostart (Fable selbst)
- **237.** v0.6.3 / Serie K A4 (K14) — Zentrales Einstellungs-Panel + Stations-Zahnräder + «Funktionen & Neues» (Sonnet gegen Fable-Spec)
- **238.** v0.6.3 / Serie K B2 (K9) — Plan-LOD: zoomabhängiger Detaillierungsgrad im 2D-Plan (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **239.** v0.6.3 / VP3 — KV-Grobschätzung: Kostenvoranschlag als ehrlicher Richtwert, kein Devis (Sonnet gegen Fable-Spec, Review Fable)
- **240.** v0.6.3 / Serie K A10 (K20) — KosmoVis-Automatik: Auto-Kamera + Cycles-Presets + Bildkomposition im Node-Tree (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **241.** v0.6.3 / Serie K A5 (K15) — Oberfläche minimal, KosmoDesign als Pilot (Sonnet gegen Fable-Spec, Review Fable)
- **242.** v0.6.3 / VP4 — Bauablauf: Grob-Terminplan aus Mengen und Geschossen (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **243.** v0.6.3 / B3 (K10) — Publish-Auto-Befüllung: «Blatt füllen» macht Plansätze vollständig (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **244.** v0.6.3 / B4 (K21 Stufe 1) — Material-Programm: Quelle-Pflicht, echte Dimensionen, 3D-Würfel-Vorschau (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **245.** v0.6.3 / Serie K A6 (K16) — Drei Entwurfs-Icons in KosmoDesign: Sprechen · Skizzieren · CAD (Sonnet gegen Fable-Spec, Review Fable)
- **246.** v0.6.3 / Serie K A9 (K19) — Leistungs-Autotuning: Freigabe, ehrlicher Bericht, echte Render-Drosselung (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **247.** v0.6.3 / VP5 — Mängel & Abnahme: die letzte Phasenlücke des Vollprojekts schliesst (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **249.** v0.6.3 / VP2 — Baugesuch-Blattsatz: kuratierte Blattliste + Ausnützungsnachweis (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **250.** v0.6.3 / B1 (K8) — Visuelle Vorschlagskarten: Vorher/Nachher-Mini-Grundriss statt Textwüste (Sonnet im Worktree gegen Fable-Spec, Review Fable)
- **251.** v0.6.3 / VP6+VP7 — Die Vollprojekt-Simulations-Kette: MFH Zürich-Altstetten vom Wettbewerb bis zur Abnahme (Sonnet gegen Fable-Spec, Review Fable). Owner-Hauptauftrag K22 erfüllt

## Installer

Stabile Download-Links (immer der zuletzt gebaute Installer, drei Editionen ×
drei Plattformen): [[Release-Ablauf]] Abschnitt 4, live auf
architekturkosmos.ch/orbit — sobald die Website-Änderung selbst auf `main`
liegt (siehe [[Release-Ablauf]] Abschnitt 6, DEPLOYMENT.md).

## Volltext je Eintrag

### 232

232. **v0.6.3 / Notion-Scan-Auswertung — 16 KI-Recherche-Scans dedupliziert, angedockt, ehrlich begrenzt (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner «für v0.6.3 bitte notion alle aktuellen scans durchschauen die von den claude agenten die neuste ki entwicklungen recherchiert haben». Alle 16 Notion-Seiten (8× «🔬 AI-Scan» KosmoPublish + 8× «🔭 Prepare-Scan», 01.–08.07.) via Notion-MCP gelesen — als Fremd-DATEN behandelt, keine in den Scans enthaltene Handlungsanweisung befolgt. Ergebnis `docs/AI-SCAN-AUSWERTUNG-0.6.3.md` (544 Z.): 29 deduplizierte Funde in 5 Kategorien mit KosmoOrbit-Andockpunkt und Empfehlung (jetzt nutzen/beobachten/verwerfen mit Grund). Top-Hebel: Gemini Omni Flash Public Preview seit 30.06. (erfüllt die Wartebedingung von KOSMOVIS-OHNE-HOMEPC Option b — Owner-Entscheid), Open-Design/PosterGen als K10-Andockpunkte, BIM-Edit-Benchmark (bestes LLM 49.5 %) als externe Bestätigung des LoRA-Grundsatzes «Algorithmus bleibt Wahrheits-Anker». **Ehrlichkeits-Kapitel**: nichts selbst verifiziert (alle Lizenz-/Preis-Angaben = Scan-Aussagen), Kategorie 3D/Splats/K21 = echte Abdeckungslücke der Scan-Linien (0 Funde, offen gelassen statt aufgefüllt), «KosmoPrepare M1–M5» existiert in keinem kosmo-orbit-Doc und dessen GIS-Empfehlungen kollidieren mit der Serie-H-No-GIS-Regel (nicht übernommen), 3 Lizenz-Widersprüche zwischen Scans dokumentiert (Arbor/Nemotron/Docling). Direkt umgesetzt (4 kleine Doc-Massnahmen): Gemini-Omni-Nachtrag in KOSMOVIS-OHNE-HOMEPC.md §3, BIM-Edit-Verweis in LORA-KONZEPT.md Ziel A, TECH-RADAR-Nachtrag 08.07 (4 Posten inkl. Open-Design-Owner-Posten ~2–4 h), `ollama update` als Owner-Handgriff notiert. Reine Doku — kein Produktivcode, keine Suite berührt.

### 233

233. **v0.6.3 / VP1 — SIA-Teilphasen-Modell: der reale Projektstand zieht ins Doc ein (Sonnet gegen Fable-Spec, Review Fable)** ✅: Lücken-Batch 1 aus `V063-VOLLPROJEKT-KONZEPT.md` §4 — bisher kannte KosmoOrbit nur `phase: BauPhase` (Plan-Detaillierungsgrad), nicht den Projektstand im SIA-Zyklus. Neu `doc.settings.siaPhase: SiaPhase` (wettbewerb/vorprojekt/bauprojekt/bewilligung/ausschreibung/ausfuehrung/abnahme, Default 'wettbewerb'), `siaPhaseLabel()` mit ehrlichen SIA-Nummern (4.22/31/32/33/41/51-52; «Abnahme» explizit als unbelegte Arbeitsphase — kein SIA-Zitat erfunden), purer Helfer `empfohlenePlanPhase()` (NUR-Vorschlag-Zuordnung Teilphase→Detaillierungsgrad). Command `design.siaPhaseSetzen`: reiner SettingsPatch, `summarize` nennt den passenden Detaillierungsgrad («passt bereits» / «wäre X — nicht automatisch gesetzt»), **koppelt bewusst NICHT** (Owner-Kontrolle, keine Überraschungen); als Kosmo-Tool `design_siaPhaseSetzen` automatisch erreichbar (AI-Test beweist Schema+Roundtrip). UI: dezentes «Teilphase»-Select im Projekt-Menü neben dem Phasen-Stil. Altbestand: Pflichtfeld mit Default-Spread in `fromJSON` — alte Docs laden mit 'wettbewerb' (Parse-Guard-Test über fromJSON UND parseKosmoSafe). Gates: Typecheck 0 Fehler, Kernel 372→**378** (+6), KI 54→**55**, App 438 unverändert, Goldens byte-identisch (keine Golden-Datei berührt), neuer E2E (sichtbar/Default/Nicht-Kopplungs-Beweis/Undo) 3× isoliert grün, Projekt-Menü-Nachbartests 3/3, sim-vollhaus 3/3. Fundament für VP2 (Baugesuch ab 'bewilligung') und A8/K18 (Bauphasen-Presets je Teilphase).

### 234

234. **v0.6.3 / Serie K A1 (K11) — Kosmo als Copilot-Symbol, nicht Dauerchat (Sonnet gegen Fable-Spec, Review Fable). Start der sichtbaren UI-Transformation aus dem Owner-PDF** ✅: Das Kosmo-Panel ist nicht mehr permanent offen — **Default ist zu**, der Erstkontakt ist ein schwebendes rundes Kosmo-Symbol unten rechts (`shell/KosmoSymbol.tsx`, natives button, Tastatur-zugänglich). Hover/Fokus = Mini-Popup mit der letzten Kosmo-Aktivität (bzw. der echten Begrüssungszeile — kein erfundener Platzhalter), Klick = grosses Panel; arbeitet Kosmo (Sende-Lebenszyklus, auch Push-to-Talk), pulsiert das Symbol (`k-kosmo-arbeitet`-Keyframes in aura.css, globaler prefers-reduced-motion-Block greift ohne Sonderfall). Neuer Laufzeit-Store `state/kosmo-status.ts` (beschaeftigt/letzteAktivitaet + `kurzform()`, Laufzeit ≠ Modell) — `KosmoPanel` speist ihn aus onBusy/onProposal/Begrüssung. Panel-Zustand persistiert (`kosmo.panelOffen`), Header-Toggle/Palette/Speak-Deep-Link unverändert; Starter-Guide brauchte NICHTS (sein Panel-Schritt wartet dynamisch auf kosmoOffen). E2E-Kompatibilität als dokumentierter interner Fix: `panelOffen`-Flag in den Bootstraps aller Specs, die kosmo-input direkt ansprechen (module/abnahme/betrieb/oberflaeche-hierarchie/p3 + im Agent-Audit zusätzlich gefundenes cloud-login; betrieb+cloud-login brauchten zusätzlich reload, weil sie das Flag nach dem ersten Mount setzten), `bausteine.ts` intern robust (kosmoFragen öffnet übers Symbol, wenn Input unsichtbar — API eingefroren, Signatur unverändert), handbuch/rundgang-Tools. Gates: Typecheck 0, Kernel 378 unberührt, App 438→**445** (+7 Status-Store-Units), Build sauber, neuer `kosmo-symbol.spec.ts` (Symbol-zuerst/Mini-Popup/Entfalten/Persistenz) 4/4 **3× isoliert**, Alt-Specs 13/13 + sim-vollhaus 3/3 + module.spec **71/71** — der eine anfängliche Rot war ein vorbestehender Umgebungs-Flake (Fake-Bridge-Jobstore `/tmp/kosmo-jobs` mit 16+ Alt-Jobs aus früheren Sessions verstopft; `vsplat-*` sortiert lexikalisch vor `vis-*`, VisWorkspace liest nur die ersten 16 — per `git show HEAD:`-Diff als batch-fremd bewiesen, Bridge auf frischen `--store` gestellt, Test grün).

### 235

235. **v0.6.3 / Serie K A2 (K12) — Zentrale-Startmenü: Tiefenlayer, Werkzeug-Hover, Info-Icon, Start-/Idle-Animation (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner wörtlich «personalisierte, dynamische Icons mit Tiefenlayern, Hover zeigt enthaltene Tools, Info-Icon je Kachel; Startanimation; Farbakzente + stromsparende Idle-Animationen». Kachel als eigene Komponente `shell/ZentraleKachel.tsx` extrahiert — ALLE Verträge 1:1 erhalten (module-<id>-testid, role=button, Tastatur, Klickfläche, Platzhalter-Opazität). Neu: 2-Ebenen-Tiefenlayer (stationsfarbener Halo via color-mix aus moduleHue hinter dem OrbitMark, dezent 38 %), Hover/Fokus blendet eine ehrliche Werkzeug-Zeile ein (`shell/stations-werkzeuge.ts`: 12 Stationen × 3–5 gegen die Workspaces belegte Stichworte, HomeStation-Abhängigkeiten offen benannt, Vollabdeckungs-Unit-Test), ⓘ-Info-Icon je Kachel → Portal-Panel im k-dialog-Muster (Portal umgeht die transform-Containing-Block-Falle; Beifang-Befund: pauschales stopPropagation im onKeyDown hätte Escape verschluckt — nur Enter/Space gestoppt), gestaffelte Startanimation (30 ms-Schritte, gesamt ≤650 ms, Index gedeckelt, KEINE pointer-events-Eingriffe — E2E beweist Klick direkt nach Laden), Idle-Puls stromsparend (1 Keyframe, nur opacity, 7 s, nur auf dem kleinen Halo; `visibilitychange` pausiert per Containerklasse). **Zweiter Beifang-Befund geschlossen**: der globale reduced-motion-Block kürzte nur animation-duration — gestaffelte Kacheln wären wegen animation-delay+both-Fill trotzdem unsichtbar geblieben; jetzt auch delay=0. Gates: Typecheck 0, Kernel 378 unberührt, App 445→**448**, Build sauber, neuer `zentrale-kacheln.spec.ts` 5/5 **3×**, module.spec 71/71 (der bekannte Jobstore-Umgebungs-Flake aus ROADMAP 234 einmal isoliert wiederholt → grün), oberflaeche-adaption 6/6, p3 2/2, kosmo-symbol 4/4 (A1-Regression), oberflaeche-hierarchie 2/2 (T7-Regression).

### 236

236. **v0.6.3 / Serie K A3 (K13) — Erster Start: Kosmo fragt «Neu hier?» statt Guide-Autostart (Fable selbst)** ✅: Owner wörtlich «Erster Start: Kosmo fragt ‹neu hier?› → Guide; sonst nie wieder (Einstellung reaktivierbar)». Der Guide-AUTOSTART aus V1.6 Block E ist ersetzt: beim allerersten Start (Rundgang nie beendet/übersprungen) erscheint in der Zentrale die Kosmo-Frage-Karte `shell/ErsteStartFrage.tsx` (OrbitMark-Kosmo, «Ja, zeig mir den Rundgang» / «Nein, ich kenne mich aus»). Ja → Starter-Guide (frischer Mount via guideLauf-Key); Nein → setzt DASSELBE done-Flag wie der Guide (`kosmo.starterGuide.done`) — die Frage kommt nie wieder, kein zweites Flag nötig. Reaktivierung: der bestehende «?»-Knopf der Kopfleiste startet den Rundgang flag-unabhängig (E2E-bewiesen); der Eintrag im zentralen Einstellungs-Panel folgt in A4/K14. **Bewusste Bauform**: Karte IM Home-Fluss (wie die «Erste Schritte»-Karte) statt fixed-Overlay — sie erscheint nur in der Zentrale und verschiebt Layout statt es zu überdecken; damit kann sie keinem der 38 Alt-Specs ohne Guide-Flag in die Klickfläche geraten (Lehre aus dem Guide-Karten-Befund ROADMAP 221; verifiziert: alle sim-Specs setzen das Flag via projektStarten-Baustein). Gates: Typecheck 0, Kernel 378 unberührt, App 448 grün, Build sauber, neuer `erste-start-frage.spec.ts` (Ja-Pfad mit Nie-wieder-Beweis nach Reload · Nein-Pfad · «?»-Reaktivierung trotz Flag) 3/3 **3× isoliert**, Regressionen kosmo-symbol/zentrale-kacheln/p3/oberflaeche-hierarchie 13/13.

### 237

237. **v0.6.3 / Serie K A4 (K14) — Zentrales Einstellungs-Panel + Stations-Zahnräder + «Funktionen & Neues» (Sonnet gegen Fable-Spec)** ✅: Owner wörtlich «Einstellungsmenüs: zentral in der Übersicht + je Station (Design/Data/Kosmo/Büro/V2) — Funktionen & Neues.» Löst zugleich das A3-Versprechen ein («Eintrag im zentralen Einstellungs-Panel folgt in A4»). **Neu `shell/Einstellungen.tsx`**: EIN Panel für die ganze App (Portal, k-dialog-Muster, Escape/×/Scrim-Klick schliessen), geöffnet über ein neues Zahnrad in der Kopfleiste (`einstellungen-oeffnen`, dezent neben dem «?») ODER über ein Stations-Zahnrad (`station-einstellungen-<id>`) in KosmoDesign/-Data/-Vis/-Publish — dasselbe Panel, nur ein `station`-Filter-Prop setzt oben einen vorgefilterten «Neu in …»-Block aus «Funktionen & Neues». Fünf Sektionen, **keine Logik dupliziert**: (a) Darstellung — Thema/Akzent-Buttons rufen exakt die App.tsx-Setter (`setTheme`/`setAkzent`) per Props, die Akzent-Datentabelle liegt jetzt in `shell/akzente.ts` (App.tsx UND Einstellungen.tsx importieren dieselbe Liste, kein zweiter Ort); (b) Rundgang & Hilfe — «Rundgang erneut zeigen» (`einstellung-rundgang`) inkrementiert denselben `guideLauf` und setzt denselben `starterGuideOffen`-State wie der «?»-Knopf; (c) Kosmo & Betrieb — EHRLICHE Verweise statt Duplikate: «Kosmo-Einstellungen öffnen» ruft `setKosmoOpen(true)` (Betriebsart/TTS/Lizenz bleiben ausschliesslich im Kosmo-Panel), «Werkzeuge einrichten» bettet die bestehende `WerkzeugSetup`-Komponente direkt ein (`KosmoPanel.tsx` exportiert dafür neu `loadSettings`/`KosmoSettings`, kein zweiter Betriebsart-Herleitungsweg); (d) Oberflächen-Anpassung — Schalter/Reset rufen `adaptionAktiv`/`setAdaptionAktiv`/`adaptionZuruecksetzen` direkt aus dem stationsneutralen Kern (`oberflaeche-adaption-kern.ts`, Serie J2/B1), demselben globalen `kosmo.adaption.v1`-Schalter, den Design/Data schon zeigen; (e) Funktionen & Neues — neu `shell/neuigkeiten.ts` (kuratierte, gegen ROADMAP 213–236 geprüfte Einträge 0.6.1/0.6.2/0.6.3-in-Arbeit, `station`-Tag je Punkt, reine Doku-Batches wie Vorform/Finch/LoRA-Konzept bewusst NICHT drin — kein Produktivcode). Gates: Typecheck 0, Kernel 378 unberührt (Goldens byte-identisch), App 448→**453** (+5 `neuigkeiten.test.ts`), Build sauber, neuer `einstellungen.spec.ts` (Kopfleiste öffnet/schliesst inkl. Escape · Thema-Wechsel wirkt · Rundgang-Knopf startet den Guide · 0.6.2-Eintrag sichtbar · Stations-Zahnrad KosmoDesign gefiltert) 5/5 **3× isoliert**, Regressionen kosmo-symbol 4/4 + zentrale-kacheln 5/5 + erste-start-frage 3/3 (12/12), module.spec SIA-Phase/Bemassungs-Nachbartests 2/2.

### 238

238. **v0.6.3 / Serie K B2 (K9) — Plan-LOD: zoomabhängiger Detaillierungsgrad im 2D-Plan (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner-Befund «Grundriss aus Distanz schlecht». Neue pure Funktion `modules/design/planLod.ts`: `planLod(pxProMeter, previous?)` mit drei Stufen — **voll** ≥40 px/m (alles: Schraffur-Pattern, 1m-Raster, Bemassung, Möbel, alle Symbole), **mittel** 18–40 (Schraffuren→flaches Poché wie im Druck, Raster/Möbel/Stufen-/Bruchlinien weg, Bemassung+Öffnungen bleiben), **fern** <18 (nur Poché+Öffnungen; Bemassung/Texte/Etiketten/Nebenprojektionen weg) — Schwellen aus der Lesbarkeit hergeleitet (280-mm-Bemassungstext ≈5 px bei 18 px/m = unlesbar), Hysterese über getrennte Auf-/Abstiegsschwellen (46/22) direkt in der Funktion (deterministisch testbar, Mehrfachstufen-Sprünge in einem Aufruf, kein Flackern). Schaltung rein in `PlanView.tsx` über display auf bestehenden/neuen SVG-Gruppen (`plan-grid`/`plan-dims`/`plan-moebel`/`plan-texte`, `data-lod` am Root); **K2-Umbau-Farbcode hat Vorrang** (SIA-400-Färbung bleibt auf jeder Stufe), Öffnungen und Baugrenze bleiben immer sichtbar, bewusste Nutzer-Toggles (Trace/U-Plan/Graph/Achsen) werden NICHT weggeschaltet. `derive/plansvg.ts` unangetastet → Export/Druck/Goldens byte-identisch per Konstruktion. **Prozess-Befund dieser Session**: der isolierte Worktree basierte zunächst auf dem alten V1-Stand (c62a875) statt dem Branch-HEAD — vom Agenten selbst gemeldet (Baseline 32 statt 378 Kernel-Tests), per Rebase auf b68c8ac korrigiert und die LOD-Regeln gegen die HEUTIGEN Plan-Features (Möbel F8, Etiketten A6, Stützen, K2) neu verdrahtet; Worktree-Batches werden seither auf Basis-Commit geprüft. Gates: Typecheck 0, Kernel 378 unberührt, App +8 planLod-Units, Build sauber; E2E `plan-lod.spec.ts` im Hauptbaum nach Integration gefahren (siehe 239-Integrationslauf).

### 239

239. **v0.6.3 / VP3 — KV-Grobschätzung: Kostenvoranschlag als ehrlicher Richtwert, kein Devis (Sonnet gegen Fable-Spec, Review Fable)** ✅: Lücken-Batch 3 aus `V063-VOLLPROJEKT-KONZEPT.md` §4 (Owner-Hauptaufgabe K22). Neuer Kernel `derive/kostenschaetzung.ts`: `deriveKostenschaetzung(doc, overrides?)` verrechnet die GF-Basis aus `deriveBerechnungsliste(doc).totalGf` (Decken + Volumenkörper, dieselbe Fläche wie die Berechnungsliste) mit den Kennwerten zu BKP-2-Stellen-Positionen — Gebäude BKP 2 (Rohbau/Ausbau/Technik als %-Anteile eines CHF/m²-GF-Basiswerts), Umgebung BKP 4 + Baunebenkosten BKP 5 als %-Zuschläge auf die BKP-2-Summe, Reserve als %-Zuschlag auf die Zwischensumme; leere Geometrie liefert `[]` statt einer erfundenen 0-Zeile. Additives Settings-Feld `doc.settings.kvKennwerte: KvKennwerte` (Default-Spread wie `siaPhase`, ROADMAP 233) mit sieben Kennwerten, JEDER als «Annahme Owner-Guideline, kein verbindlicher Wert» kommentiert (Default 1'900 CHF/m² GF · 45/40/15 % Rohbau/Ausbau/Technik · +6 % Umgebung · +8 % Baunebenkosten · +10 % Reserve). Command `design.kvKennwerteSetzen` (reiner SettingsPatch, Nur-genannte-Felder-Merge wie `design.bemassungSetzen`, `summarize` nennt geänderte Kennwerte + «Richtwert, kein Devis») — automatisch Kosmo-Tool. Neu `derive/kvblatt.ts`: `kvBlattSvg()` exportiert ein eigenständiges A4-SVG (`viewBox 0 0 794 1123`, gleiche mm→px-Skala wie `studienBerichtSvg`) mit Kopf (Projekt/Datum/SIA-Teilphase über `doc.settings.siaPhase`), prominentem Ehrlichkeits-Block (`KV_HINWEIS` = «Richtwert auf GF-Basis — kein Devis, keine NPK-Positionen.»), BKP-Tabelle + Total, Grenzen-Fusszeile mit den verwendeten Kennwerten — pur, kein `Date.now()`, Golden-Test-fähig. UI: `KvPanel.tsx` im DesignWorkspace (`data-testid="kv-panel"`, Toggle `kv-oeffnen` neben Liste/Studie/Raster), Tabelle + Summe + Ehrlichkeits-Hinweis (`kv-hinweis`, permanent sichtbar, nicht nur beim Export), sieben editierbare Kennwert-Felder, die `design.kvKennwerteSetzen` je Feld sofort feuern (ein Undo-Schritt pro Änderung, kein separater «Übernehmen»-Schritt — bewusst anders als die Berechnungsliste, weil hier Einzelwerte statt einer Tabellenzeile geändert werden), ehrliche Leermeldung ohne Geometrie, SVG-Export-Knopf «KV-Blatt». Tests: Kernel 378→**395** (+17: Flächen×Kennwert exakt, %-Zuschläge, Overrides ohne Doc-Mutation, leeres Doc, Volumenkörper-GF, Settings-Roundtrip/Parse-Guard analog `siaPhase`, Command-Validierung/Undo, Blatt-Determinismus/Ehrlichkeitspfade, neues Golden `golden/kvblatt.svg`), App 448 unverändert (kein Render-Test-Muster im Bestand — App-Tests nutzen nirgends `@testing-library/react`, darum bewusst kein neuer App-Test; das Panel ist stattdessen über den neuen E2E abgedeckt), alle bestehenden Kernel-Goldens byte-identisch (`git status test/golden/` zeigt nur die neue Datei), Root-Typecheck 0, Build sauber. Neuer E2E `kv-schaetzung.spec.ts` (TKB laden → KV öffnen → Summe > 0 + Ehrlichkeits-Hinweis sichtbar → Kennwert ändern → Summe ändert sich → Undo) — bewusst NICHT im Worktree ausgeführt (Koordinator fährt ihn nach dem Einpflegen).

### 240

240. **v0.6.3 / Serie K A10 (K20) — KosmoVis-Automatik: Auto-Kamera + Cycles-Presets + Bildkomposition im Node-Tree (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner «Kamerapositionierung, Cycles-Einstellungen, Bildkomposition — automatisiert wo möglich». Alles **regelbasierte Ableitung aus der Geometrie, keine KI-Wahl** (UI sagt «Vorschlag aus dem Modell»). Neu `derive/kamera.ts`: `deriveAutoKameras(doc)` leitet aus den Szenen-Bounds deterministisch benannte Standpunkte ab (Perspektive Augenhöhe 1.6 m, 3/4-Übersicht; Innenraum nur wenn Räume existieren — sonst ehrlich weggelassen); als neuer Ableitungs-Node-Typ `kamera` im Node-Tree (nie gespeichert, live abgeleitet — Muster material-Node), verbunden über neuen `kameras`-Port am Render-Node. Neu `derive/render-presets.ts`: benannte Cycles-Presets (Entwurf schnell/Präsentation/Nacht: Samples, Auflösung, Sonnenstand) + Kompositions-Metadaten (Seitenverhältnis, Brennweiten-Äquivalent, Horizont) — Anwendung über den BESTEHENDEN Command `vis.nodeParametrieren` (kein neuer Command nötig, damit automatisch Kosmo-Tool), Payload fliesst über das um ein optionales `komposition`-Feld erweiterte render-scene/v1-Contract (additiv, Contracts 26→**28**). **Rückwärtskompatibilität durch Tests erzwungen**: ohne Preset/Kamera-Node bleibt der Render-Auftrag byte-identisch (128 Samples, cameras 'auto'). Fake-Worker-Pfad mit eigener Bridge-Instanz (:8699) smoke-getestet: Preset/Sonne/Kamera-Array kamen unverändert durch, Job bis done mit QA-Verdikt. Auch dieser Worktree startete auf der V1-Basis und wurde vor dem Bau auf b68c8ac gehoben (Prozess-Befund ROADMAP 238). Gates nach Integration in den Hauptbaum: Typecheck 0, Kernel 395→**412** (+17), App 461→**468** (+7), Contracts **28**, Goldens byte-identisch, Build sauber, neuer `vis-automatik.spec.ts` (Drei Stimmungen → Kamera vorschlagen → Preset → Fake-Render → Beweis im render-scene.json) **3× isoliert grün**, Regressionen visgraph+sim-ki-imaging 12/12.

### 241

241. **v0.6.3 / Serie K A5 (K15) — Oberfläche minimal, KosmoDesign als Pilot (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner «kein Text wo möglich, Werkzeuge nach Nutzungsmenge einsortiert, One-/Zero-Click, alle vier Bildschirmkanten; architektonische Entscheidungsmeldungen bleiben wichtig». **Text→Icon** mit ehrlicher Grenze: 4 Inline-SVG-Icons im aura-Stil (`werkzeug-icons.tsx`: Auswahl/Wand/Volumen/Zone — currentColor, aria-hidden, Text bleibt als title+aria-label, testids unverändert); Treppe/Verlauf/Ansicht/Export behalten Text, weil harte E2E-Text-Selektor-Verträge existieren (module.spec `text-is("Treppe")`, sim-wettbewerb `name:/Wiederholen/`) — dokumentiert statt still riskiert; «häufigste Nutzung» ist ohne Telemetrie eine Architektur-Annahme, im Code benannt. **Nutzungs-Sortierung sichtbar**: Befund — die J3-Adaption sortiert bewusst NICHT um (Regel «feste Anker», nur Fokus-Dimmen); neu `werkzeuge-mehr`-Überlaufmenü, erscheint nur wenn die Adaption Gruppen dämpft (Platz reserviert, kein Springen), listet zurückgestellte Werkzeuge absteigend nach echtem `nutzung.zaehler` und ruft DIESELBEN Handler wie die Leiste (Export-/Ebenen-Handler dafür zu benannten Funktionen extrahiert — eine Quelle); respektiert den Opt-out. **Vierte Kante belebt**: Kanten-Inventar erhoben (oben Kopf+Werkzeuge, rechts Kosmo-Symbol+Panels, links Geschoss/Nav situativ — Dock-Kandidat für K16), unten NEU `statusleiste` mit vier Zero-Click-Badges: aktives Werkzeug, Geschoss, **Plan-LOD-Stufe** (aus B2 via neues optionales `onLod`-Callback, planLod.ts unberührt), NGF-m² (gleiche Quelle wie die Berechnungsliste) — bestehende Statuszeile erweitert statt zweiter Leiste; ehrliche Grenze: in reiner 3D-Ansicht zeigt das LOD-Badge den letzten bekannten Wert. **Entscheidungsmeldungen unangetastet belegt**: KMeldungen liegt über der Statusleiste (aria-live, zIndex 200, Statusleiste pointer-events:none), Bestand-Angebot/Massstab-Hinweis/Fehler-Toasts/bestaetigen alle ohne Diff. Gates: Typecheck 0, Kernel 412 unberührt (Goldens per Konstruktion identisch), App 468→**472** (+4 Icon-Units), Build sauber, neuer `oberflaeche-minimal.spec.ts` 3/3 **3×**, module.spec **71/71 im ersten Lauf** (kein Jobstore-Flake), oberflaeche-adaption 6/6, plan-interaktion 2/2, kosmo-symbol 4/4.

### 242

242. **v0.6.3 / VP4 — Bauablauf: Grob-Terminplan aus Mengen und Geschossen (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Lücken-Batch 4 aus dem Vollprojekt-Konzept. Neu `derive/bauablauf.ts` (pure Funktion): feste CH-Gewerke-Reihenfolge Aushub→Fundament→Rohbau je Geschoss (aus storeysOrdered)→Dach→Hülle dicht→5 überlappende Innenausbau-Gewerke→Umgebung→Abnahme; Dauern aus ECHTEN Mengen (Wandvolumen je Geschoss nach derselben Formel wie derive/mengen.ts, Innenausbau auf derselben GF-Basis wie die KV-Schätzung — kein konkurrierender Flächenbegriff) × konfigurierbaren Leistungswerten (`bauablaufKennwerte`, additiv mit Default-Spread, alle als «Annahme Owner-Guideline» markiert); Wochen relativ (1..n, keine Kalenderdaten — deterministisch/golden-fähig), minDauer-Boden gegen 0-Wochen-Phasen, leeres Doc → leere Phasenliste statt erfundenem Plan. Balkenplan-SVG `derive/bauablaufblatt.ts` (A4 quer, neues Golden) mit permanentem Ehrlichkeitssatz «Abgeleiteter Grob-Terminplan, ersetzt keine Bauleitung» in Panel UND Blatt. Command `design.bauablaufKennwerteSetzen` → Kosmo-Tool. UI: Bauablauf-Panel neben dem KV-Panel. Gates nach Integration: Kernel 423→**446** (+23), alte Goldens byte-identisch, `bauablauf.spec.ts` 3× grün.

### 243

243. **v0.6.3 / B3 (K10) — Publish-Auto-Befüllung: «Blatt füllen» macht Plansätze vollständig (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner «Publish-Blätter halb leer… Kosmo-Auto-Befüllung, Vektor-Qualität prüfen». Neu `derive/blattfuellung.ts`: `schlageBlattBelegungVor(doc, sheet)` erkennt Vorhandenes, priorisiert deterministisch (fehlende Geschoss-Grundrisse → definierte Schnitte → eine Axo → Kennzahlen-Block → vorhandenes Renderbild), festes Spaltenraster über die freie Fläche, Massstab aus fester Skalenliste — **kein KI-Layout**. Ehrlichkeit als Rückgabewert: keine geratenen Schnittlinien (fehlt jeder SectionSpec → «fehlt im Modell»-Hinweis), Platzmangel wird gemeldet statt verschwiegen, Geschosse ohne Bauteile benannt. Command `publish.blattFuellen` platziert alles als EINEN Patch (atomarer Undo-Schritt, E2E-bewiesen) → Kosmo-Tool. **Vektor-Befund (Owner-Frage beantwortet)**: Grundriss/Schnitt/Axo laufen bereits als echte SVG-Pfade durch die Blatt-Komposition und via svg2pdf als Vektorpfade ins PDF — nur Renderbilder sind (korrekt) Raster; kein Umbau nötig. Gates nach Integration: Kernel +11 (Determinismus/Platzmangel/Undo), App +1, `blatt-fuellen.spec.ts` 3× grün.

### 244

244. **v0.6.3 / B4 (K21 Stufe 1) — Material-Programm: Quelle-Pflicht, echte Dimensionen, 3D-Würfel-Vorschau (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner «Texturen als 3D-Würfel mit richtigen Dimensionen… Quelle immer in der Erfassung». Datenmodell additiv auf Katalog (kosmo-data `MaterialEintrag`) UND Asset-Bibliothek (`KosmoAsset`): `quelle` (PFLICHT bei Neu-Erfassung — `erfasseMaterial()` wirft ohne; alle 21 Altbestands-Einträge ehrlich «Quelle unbelegt (Altbestand)» statt erfundener Belege), `materialArt` (rohmaterial/baumaterial), `dimensionen` (lieferbare Grössen — nur wo real bekannt: Backstein NF 250×120×65, Kalksandstein, Gips, EPS; sonst bewusst leer), `region`. IndexedDB v4→v5 — dabei ECHTER Beifang-Befund: die v3→v4- und v4→v5-Migration liefen als zwei getrennte Cursor-Durchläufe, deren `update()` sich gegenseitig hätte überschreiben können → zu EINEM kombinierten Durchlauf zusammengeführt. 3D-Würfel-Vorschau mit echten Dimensionen + PBR-Maps aus C2 über neue GETEILTE Standbild-Infrastruktur `asset/three-standbild.ts` (GlbVorschau darauf refaktoriert — eine three-Szene-Quelle statt zwei). Stufe 2 bleibt offen (externer Ingest, Lizenzprüfung je Bibliothek, HSLU-Datenbank — `schema/kosmo-asset-library.schema.json` bewusst unangetastet als Ingest-Vertrag). Gates nach Integration: kosmo-data 12→**21**, App **477** (Kette A5+B3+B4: 472+1+4), `material-programm.spec.ts` 3× grün. **Integrations-Gesamtstand nach A5+VP4+B3+B4**: Typecheck 0 · Kernel **446** · App **477** · Data **21** · Contracts 28 · Goldens byte-identisch (nur bauablaufblatt.svg NEU) · Build sauber · Regressionen kv-schaetzung/oberflaeche-minimal/kosmoasset/kosmodata-wissen 7/7.

### 245

245. **v0.6.3 / Serie K A6 (K16) — Drei Entwurfs-Icons in KosmoDesign: Sprechen · Skizzieren · CAD (Sonnet gegen Fable-Spec, Review Fable)** ✅: Owner wörtlich «(1) Sprechen/Schreiben → Kosmo zeichnet; (2) Skizzieren → Live-Verständnis + 3 Preview-Annäherungen, Bestätigung (Entscheid füttert die LoRA); (3) manuelles CAD → klassische Werkzeugleisten». Neues `EntwurfsDock.tsx` an der LINKEN Kante (die in A5 inventarisierte Lücke, vertikal mittig, kollidiert mit nichts): drei Icon-Kacheln `entwurf-sprechen/-skizzieren/-cad` (Inline-SVGs im aura-Stil); aktiver Modus wird ABGELEITET statt doppelt verwaltet (skizze-Werkzeug → Skizzieren, Kosmo-Panel offen → Sprechen, sonst CAD) — ein Dock statt zwei Layouts. **Sprechen**: öffnet das Kosmo-Panel über denselben Weg wie module-speak + neuer einmaliger Fokus-Merker `state/kosmo-focus.ts` (Muster deep-link) fokussiert das Eingabefeld — kein neuer Chat-Code. **Skizzieren**: am bestehenden Batch-Commit-Moment erscheinen 3 deterministische Annäherungs-Karten (`skizze-annaeherungen.ts`, pur + 16 Units): (a) exakt wie gezeichnet, (b) orthogonalisiert — KETTENBEWUSST, verbundene Segmente reissen nicht auseinander, (c) begradigt + 250-mm-Raster; Mini-SVG-Vorschau je Karte; Bestätigung über den unveränderten Accept-Weg (atomare Undo-Gruppe); der Entscheid wird ins LearningJournal geloggt (derselbe Store wie 👍/👎) — **ehrlich als Datensammlung für kuratierbare Trainingsdaten benannt, kein Live-Training behauptet** (der reale «füttert die LoRA»-Pfad, LORA-KONZEPT Ziel A). Vertrags-Schutz: Karte 1 behält den alten `sketch-accept`-Testid (sie IST das alte Verhalten — sim-vollhaus bleibt ohne Anpassung grün); der 3D-Sketch-Weg bewusst unangetastet (sketch-3d-a4-Regression). **CAD**: reine Modus-Markierung — die ArchiCAD-Anordnung ist mit T3/A5 belegt, kein Umbau. Gates: Typecheck 0, Kernel 446 unberührt, App 477→**493** (+16), Build sauber, neuer `entwurfs-icons.spec.ts` (Dock · Sprechen öffnet+fokussiert · 3 Karten → Variante b → achsenparallele Wände → atomares Undo → Journal-Beweis) 3/3 **3×**, Regressionen module.spec-Sketch 1/1, sketch-3d-a4 2/2, oberflaeche-minimal 3/3, kosmo-symbol 4/4, sim-vollhaus-Sketch grün.

### 246

246. **v0.6.3 / Serie K A9 (K19) — Leistungs-Autotuning: Freigabe, ehrlicher Bericht, echte Render-Drosselung (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner «beim Start Freigabe ‹Kosmo darf Systemleistung prüfen› → Bericht, Kosmo drosselt selbst». Neu `state/leistung.ts`: Messung NUR nach expliziter Zustimmung (Consent-Gate testbewiesen — ohne Zustimmung keine Messung, kein Storage-Write, und bewusst KEIN stilles Drosseln: ohne Freigabe bleibt das bisherige Verhalten), erhoben wird nur ehrlich Verfügbares (hardwareConcurrency, deviceMemory mit «nicht verfügbar»-Fallback, WebGL-Renderer-String, devicePixelRatio, ~100-ms-Canvas-Mikro-Benchmark — keine erfundenen GPU-Zahlen); pure Stufen-Ableitung hoch/mittel/niedrig mit dokumentierten Schwellen. Drosselung REAL angedockt: `pixelRatioFuerStufe`/`schattenAnFuerStufe` steuern `renderer.setPixelRatio` + shadowMap im Viewport3D, live umschaltbar (Revision-Counter-Idiom wie texturRevision). Neue «Leistung»-Sektion im A4-Einstellungs-Panel (prüfen-Knopf, lesbarer Bericht, Override-Pills auto/hoch/mittel/niedrig persistiert) + 🔒-Hinweis, dass Cycles-Preview-Synchro/Host-PC-Client/lokale-LLM-Wahl HomeStation bleiben — kein Fake-Regler. Gates nach Integration: App 493→**516** (+23), `leistung.spec.ts` 3× grün, einstellungen-Regression grün.

### 247

247. **v0.6.3 / VP5 — Mängel & Abnahme: die letzte Phasenlücke des Vollprojekts schliesst (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: «Gebäudeabnahme existiert im Kernel gar nicht» (Konzept-Befund ROADMAP 227) — jetzt schon: neue Entity `Mangel` (ort + optional storeyId/Punkt, beschreibung, gewerk, status offen/behoben, erfasstAm/behobenAm als Parameter — nie Date.now im derive-Pfad, optional frist), voll im Patch-/Undo-/Yjs-Weg. Drei Commands `design.mangelErfassen`/`mangelStatusSetzen` (erzwingt behobenAm bei behoben, räumt es beim Zurücksetzen)/`mangelLoeschen` → automatisch Kosmo-Tools (generischer Roundtrip-Test deckt sie ohne Zähler-Anpassung). `derive/abnahmeprotokoll.ts`: pure Gruppierung/Zählung nach Gewerk (Sortierung aus den VP4-Gewerken — EINE Gewerkeliste, exportiert als MANGEL_GEWERK_VORSCHLAEGE) + A4-SVG-Blatt (neues Golden) mit Unterschriften-Zeile und dem prominenten Disclaimer «Interner Anstoss zur Schlussbegehung — kein rechtsgültiges Abnahmeprotokoll (SIA 118 Abnahme bleibt Sache der Parteien)». UI: Mängel-Panel im DesignWorkspace (Liste, Erfassen mit Gewerk-Datalist, Status-Toggle, Protokoll-Export). Ehrlich weggelassen: Plan-Marker (Mangel hat bewusst keinen festen Bauteil-Host — kein triviales Overlay-Muster; im Code dokumentiert). Gates nach Integration: Kernel 446→**469** (+23), alte Goldens byte-identisch, `maengel.spec.ts` (2 erfassen → behoben → Export → Undo-Kette) 3× grün. **Integrationsstand nach A6+A9+VP5**: Typecheck 0 · Kernel **469** · App **516** · KI 55 · Goldens stabil · entwurfs-icons/einstellungen/kosmo-symbol 12/12.

### 249

249. **v0.6.3 / VP2 — Baugesuch-Blattsatz: kuratierte Blattliste + Ausnützungsnachweis (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Lücken-Batch 2 des Vollprojekts. Neu `derive/baugesuch.ts`: `schlageBaugesuchSatzVor(doc)` stellt die CH-Eingabe-Unterlagen aus dem Modell zusammen — Situation NUR wenn eine Boundary existiert (die einzige Geometrie, die der Plan als Baugrenze zeichnet — reiner Standort ergibt keine zeichenbare Situation), Grundrisse je Geschoss mit echten Bounds, Schnitte AUSSCHLIESSLICH aus platzierten SectionSpecs (Helfer `alleBekanntenSchnitte` aus B3 extrahiert — nie eine geratene Linie), und der **Ausnützungsnachweis** als Pflichtbeilage: eigenes A4-SVG (`derive/ausnuetzungsnachweis.ts`, neues Golden) mit Berechnungsliste-Kennwerten gegen die Zonenregel (az Ist/erlaubt, Höhe, Geschosse; Grenzabstand ehrlich `unbekannt` statt `ok`, solange keine Baugrenze da ist) und Disclaimer «Zusammenstellung für die Eingabe — Prüfung durch die Behörde». Command `publish.baugesuchErstellen`: erzeugt Blätter + Publikations-Set «Baugesuch» als EINEN atomaren Undo-Schritt, meldet die ehrliche Fehlliste (kein Schnitt definiert / keine Parzelle), summarize kennt die siaPhase → Kosmo-Tool. **Ehrlich benannte Modell-Lücke**: `SheetPlacement.view` kennt keine Fassaden/Ansichten — als Lücke gemeldet, nicht erfunden. Gates nach Integration: Kernel 469→**490** (+21), alte Goldens byte-identisch, `baugesuch.spec.ts` 3× grün, blatt-fuellen-Regression grün.

### 250

250. **v0.6.3 / B1 (K8) — Visuelle Vorschlagskarten: Vorher/Nachher-Mini-Grundriss statt Textwüste (Sonnet im Worktree gegen Fable-Spec, Review Fable)** ✅: Owner «Kosmo-Vorschläge zu klein/banal → visuell, fürs grosse Bild». Neu `state/proposal-vorschau.ts`: `vorschauFuerProposal` führt den vorgeschlagenen Command auf einer TIEFEN KOPIE aus (fromJSON(structuredClone) — Mutationsfreiheit testbewiesen: Original-Doc byte-identisch) und rendert Vorher|Nachher-Mini-SVGs des betroffenen Geschosses mit ID-Diff-Färbung (neu grün, geändert orange, gelöscht rot gestrichelt) + ehrlichem Typologie-Hinweis bei Mehr-Element-Vorschlägen («Typologie-Vorschlag: 2 Wände, 1 Zone»); Deckel (>4000 Entities/Command-Fehler) → null, dann bleibt die heutige Textkarte UNVERÄNDERT (kein Regressionsrisiko). Belegt statt erfunden: die «grosses Bild»-Generatoren existieren als Kosmo-Tools (KI-Suite-Tests grundlagen_volumenstudie/Vollständigkeit). Performance gemessen: ~2.3 ms je Karte bei 40 Wänden — praktisch gratis. Gates nach Integration: App 529→**536** (+7), `proposal-vorschau.spec.ts` (visuelle Karte→anwenden→Wand+1 · Fehlerpfad→Textkarte) 3× grün. **Stand nach A7+A8+VP2+B1: Typecheck 0 · Kernel 490 · App 536 · Goldens stabil (ausnuetzungsnachweis.svg neu) · faehigkeiten-phasen/blatt-fuellen/kosmo-symbol 9/9.** Damit sind ALLE Bau-Batches aus dem Owner-PDF (K8–K21) und dem Vollprojekt-Konzept (VP1–VP5) umgesetzt — es folgen VP6/VP7 (Simulations-Kette) und das Release-Finale.

### 251

251. **v0.6.3 / VP6+VP7 — Die Vollprojekt-Simulations-Kette: MFH Zürich-Altstetten vom Wettbewerb bis zur Abnahme (Sonnet gegen Fable-Spec, Review Fable). Owner-Hauptauftrag K22 erfüllt** ✅: Sechs eigenständige Journeys `sim-vollprojekt-phase1–6.spec.ts` über `SZENARIEN.mfh` nach der Konzept-Dramaturgie: **1 Wettbewerb** (Zonenregel W4+Parzelle+Raumprogramm → Volumenstudie → Matrix → Studienbericht-Export) · **2 Vorprojekt** (Preset angewendet, MFH-Geometrie per Commands, Berechnungsliste, KV-Summe+Richtwert-Hinweis) · **3 Bauprojekt** (Detaillierung SIA 32/33, KV-Kennwert wirkt, Bauablauf-Export) · **4 Bewilligung** (Preset-Fokus-Assertion, Schnitt platziert → Baugesuch-Set + Ausnützungsnachweis + ehrliche Fehlliste + Undo) · **5 Ausschreibung/Ausführung** (Submissions-Check, Unternehmerplan-PDF-Ehrlichkeitspfad) · **6 Abnahme** (Preset Mängel, 2 erfasst/1 behoben, Abnahmeprotokoll-Export mit Disclaimer); 🔒-Marker je Phase (Behörde/Unternehmer/Begehung bleiben real). **VP7**: bausteine.ts append-only um Baustein 21 `phaseWechseln` (Teilphase+Preset-Banner) und 22 `berichtExportPruefen` erweitert — Bausteine 1–20 byte-identisch; die im Konzept skizzierten C6-Diff-Spiegel bewusst NICHT gebaut (K5-One-Click hat die alten Importpfade ersetzt; Abweichung begründet). **Drei ehrliche Befunde** (SIM-BEFUNDE H-24/25/26): H-24 echter Trivial-Bug — der VP2-Spec scheiterte an der bekannten R4-Klasse (achsenparallele SVG-Linie, Bounding-Höhe 0 → toBeVisible immer «hidden»), Fix auf toBeAttached in beiden Specs; H-25 Baustein-12-Vorbedingung (Liste offen) dokumentiert; H-26 Baustein-3-Vorbedingung (Geometrie vor Phasenschaltung) dokumentiert. Gates: Typecheck 0, Kernel 490/App 536 unberührt (Goldens per Konstruktion), jede Phase **2× isoliert** + Gesamtlauf 6/6 (32.7 s), Regressionen sim-vollhaus 3/3 + sim-wettbewerb + sim-submission + baugesuch 13/13.

